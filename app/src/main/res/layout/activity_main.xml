<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NewUserActivity">

    <LinearLayout
        android:id="@+id/home_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!--
            "MaterialToolbar"
            Tambien conocida como "action bar" o "app bar" es la barra superior donde estara ubicado el icono del menu desplegable
        -->
        <!--
            ?attr/ referencias a atributos. Los atributos son valores especificados
             en el tema de una aplicación. Para esta activity el tema se llama
             "AppTheme" y hereda de "Theme.MaterialComponents.DayNight.NoActionBar"

             Android también tiene sus propios atributos que se pueden usar con
             ?android:attr/.
        -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize" >

        </com.google.android.material.appbar.MaterialToolbar>

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/frModulos"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

    <!--
        "NavigationView"
        Es el recuadro que sale de la parte izquierda de la pantalla, en donde
        estaran las opciones de navegacion
    -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/header_menu"
        app:itemIconTint="@drawable/sidebar_items"
        app:itemTextColor="@drawable/sidebar_items"
        app:menu="@menu/menu_pokeaprendo"/>


  

</androidx.drawerlayout.widget.DrawerLayout>